@using System.Security.Principal
@model IEnumerable<VacationManager.WEB.Models.UserViewModel>

@{
    ViewBag.Title = "Admin Menu";
}

<h2>Admin Menu</h2>
@foreach (var user in Model)
{
    <p>@user.Email</p>
    foreach (var role in user.Roles)
    {
        <p>@role.Name</p>
    }
}
